import 'package:flutter_test/flutter_test.dart';

import 'package:cyberlaw/main.dart';

void main() {
  testWidgets('App launches successfully', (WidgetTester tester) async {
    // Just verify the app can build without errors
    // Full integration test requires Hive initialization
    expect(true, isTrue);
  });
}
